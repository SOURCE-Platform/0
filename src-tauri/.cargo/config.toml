# Dev-only: sign the SOURCE binary before `cargo run` / `tauri dev` starts
# it, so the vault helper's peer check accepts it. See the script header.
[target.aarch64-apple-darwin]
runner = "../scripts/dev-run-signed.sh"
